from pathlib import Path


def main(reference: Path, r1: Path, threads: int, output: Path) -> None:
    # build_index(reference)
    # bwa_mem(reference, r1, threads, output)
    # index_bam(output)
    raise NotImplementedError
